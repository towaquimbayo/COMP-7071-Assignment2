@model IEnumerable<_7071Sprint1Demo.Models.RentHistory>

@{
    ViewData["Title"] = "Rent History";
    int count = Model.Count();
}

<div style="margin-top:7rem; min-height:80vh;">
    <div class="d-flex justify-content-between mb-4">
        <div class="d-flex gap-4">
            <h1>Rent History</h1>
            <div class="px-4 py-2 bg-primary bg-opacity-10 justify-content-center align-content-center" style="border-radius: 1rem;">
				<h3 class="fw-normal mb-0 text-primary">@count</h3>
            </div>
        </div>
        <a asp-action="Create"
            class="btn btn-primary px-3 py-2"
            style="height:fit-content;font-size:1.1rem;border-radius:0.5rem;"
        >Create New</a>
    </div>

    @if (Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Asset</th>
                    <th>Renter</th>
                    <th>Old Rent Amount</th>
                    <th>New Rent Amount</th>
                    <th>Effective Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Asset.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Renter.Name)
                    </td>
                    <td>
                        $@item.OldRentAmount.ToString("C")
                    </td>
                    <td>
                        $@item.NewRentAmount.ToString("C")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EffectiveDate.ToString("yyyy-MM-dd"))
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="text-decoration-none">
                            <i class="bi bi-pencil"></i> Edit
                        </a> |
                        <a asp-action="Details" asp-route-id="@item.Id" class="text-decoration-none">
                            <i class="bi bi-eye"></i> Details
                        </a> |
                        <a asp-action="Delete" asp-route-id="@item.Id" class="text-decoration-none" style="color:#eb1c1c;">
                            <i class="bi bi-trash"></i> Delete
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
	}
	else
	{
        <p class="text-muted">Hmm ... no results found. Please create a new Rent History to get started.</p>
	}
</div>